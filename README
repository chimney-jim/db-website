This will be a website that will allow one to write to a database and potentially 
view items in a database.

TODO:

--make everything run on the over port 80

--SPECIAL: make ouput to same page as input
--SPECIAL: make submit button call a javascript function to insert data into the table
